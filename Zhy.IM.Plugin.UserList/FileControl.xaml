<UserControl x:Class="Zhy.IM.Plugin.UserList.FileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Margin="0,0,10,10"
             d:DesignHeight="60" d:DesignWidth="300"
             Loaded="UserControl_Loaded_1">
    <StackPanel>
        <TextBlock Height="23" Name="txtFileName" Text="{Binding FileName}"></TextBlock>
        <ProgressBar Name="pbProgress" Height="10" Maximum="{Binding ProgressMaximum}" Value="{Binding ProgressValue}"></ProgressBar>
        <TextBlock Height="25" Name="txtProgress" TextAlignment="Right" Text="{Binding ProgressText}"></TextBlock>
    </StackPanel>
</UserControl>
